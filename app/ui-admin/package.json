{
  "name": "moderman-admin",
  "version": "0.0.0",
  "description": "A content moderation platform",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "ui5 build --clean-dest",
    "start": "ui5 serve --port 8081"
  },
  "devDependencies": {
    "@ui5/cli": "^3.4.0",
    "rimraf": "^5.0.1",
    "ui5-middleware-cap": "^3.0.0",
    "ui5-middleware-livereload": "^3.0.0"
  }
}